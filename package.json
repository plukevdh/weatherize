{
  "name": "weatherize",
  "version": "1.0.0",
  "description": "Add your weather status to your Slack profile once an hour",
  "main": "index.js",
  "scripts": {
    "build": "babel src -d lib",
    "watch": "babel src -w -d lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luke van der Hoeven <hi@plukevdh.me> (http://blog.plukevdh.me)",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.13.1",
    "dotenv": "^2.0.0",
    "ramda": "^0.22.1"
  },
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-preset-es2015": "^6.13.2"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
